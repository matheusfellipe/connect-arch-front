import { ButtonProps } from 'antd';
import { Button as AntButton } from 'antd';
import styled from 'styled-components';

export const ButtonCustom: React.FunctionComponent<ButtonProps> = styled(
  AntButton,
)`
  
`;
