import { LogoContainer, LogoCustom } from './style'

type Props = {}

const TopFarmLogo = (props: Props) => {
  return (
    <LogoContainer>
    <LogoCustom small/>
    </LogoContainer>
  )
}

export default TopFarmLogo